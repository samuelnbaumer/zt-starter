# ==========================
# Resource API Service
# ==========================

# The shared JWT secret used to verify tokens issued by the IdP.
# In real environments this would be distributed securely or fetched from JWKS.
JWT_SECRET=dev-secret-idp
JWT_ALG=HS256

# Optional: Context evaluation parameters
# Simulated business hours for context check (7–19)
BUSINESS_HOURS_START=7
BUSINESS_HOURS_END=19

# Optional: Sensitive paths (comma-separated)
SENSITIVE_PATHS=/export,/admin

# Default host/port for this FastAPI app
HOST=0.0.0.0
PORT=8000
